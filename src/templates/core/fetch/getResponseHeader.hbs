const getResponseHeader = (response: Response) => {
	const headerMap:Record<string, string> = {};
	response.headers.forEach((value, key) => {
        if(value){
            headerMap[key] = value;
        }
    })
    return headerMap;
};
